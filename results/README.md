# Results

`summary/` contains compact copies of the values reported in the thesis. They
support auditing of table transcription but do not replace raw experiment
outputs.

Generated predictions, loss histories, checkpoints, latent arrays, and plots
are ignored by Git. Each experiment writes its own configuration and metrics
to the selected output directory.
